# MinIO project settings

MY_MINIO_USER=${MY_MINIO_USER:-minio}
MY_MINIO_PASSWORD=${MY_MINIO_PASSWORD:-minio123} # at least 8 chars
MY_MINIO_PORT=${MY_MINIO_PORT:-9011}

# Internal settings

COMPOSE_PROJECT_NAME=minio

# MinIO
MINIO_ROOT_USER=${MY_MINIO_USER}
MINIO_ROOT_PASSWORD=${MY_MINIO_PASSWORD}
MINIO_HOST=minio
MINIO_PORT=9000
MINIO_CONSOLE_PORT=9001

EXPOSED_MINIO_CONSOLE_PORT=${MY_MINIO_PORT}
