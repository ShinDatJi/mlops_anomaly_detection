# Modeling default environment

CONFIG_FILE=./models/${CATEGORY}/config.json

DATA_RAW_PATH=./data/raw/${CATEGORY}
VIRTUAL_DATA_RAW_PATH=${DATA_RAW_PATH}

DATA_PATH=./data/processed/${CATEGORY}

DATA_CLEAN_DB=clean.csv
DATA_TRAIN_DB=train.csv
DATA_TEST_DB=test.csv

DATA_TRAIN_DIR=train
DATA_TEST_DIR=test
DATA_TEST_PATCHING_DIR=test_patching

REPORTS_PREPROCESSING_PATH=./reports/modeling/${CATEGORY}/preprocessing
REPORTS_TRAINING_PATH=./reports/modeling/${CATEGORY}/training
REPORTS_EVALUATION_PATH=./reports/modeling/${CATEGORY}/evaluation

REPORTS_REPORT=report.json
REPORTS_MODEL=model.keras

MODELS_PATH=./models
MODELS_CONFIG=config.json
MODELS_REPORT=report.json
MODELS_MODEL=model.keras

TRACKING_URI=http://mlflow:5000

# MAX_EPOCHS=3